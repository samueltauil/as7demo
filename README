AS 7 Demo Game

The main idea of this project is to have 6 instances running on Domain mode inside JBoss AS 7.
Also the user will be able to stop all of them, but the app will start every time he stops,
so the challenge is stop all instances before JBoss starts another one.

